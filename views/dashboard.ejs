<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard - Invite and Earn</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
  <div class="container py-5">
    <h1 class="mb-4">Welcome, <%= user.name %>!</h1>
    <div class="card p-4 shadow-sm">
      <h3>Your Balance: <strong>Rs. <%= user.balance %></strong></h3>
      <p>Phone: <%= user.phone %></p>
      <p>Referral: <%= user.referrerId ? user.referrerId.name : 'None' %></p>
      <hr />
      <h5>Transactions:</h5>
      <ul class="list-group">
        <% user.transactions.forEach(tx => { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <%= tx.type.charAt(0).toUpperCase() + tx.type.slice(1) %> - Rs. <%= tx.amount %>
            <span class="badge bg-success"><%= tx.status %></span>
          </li>
        <% }) %>
      </ul>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
